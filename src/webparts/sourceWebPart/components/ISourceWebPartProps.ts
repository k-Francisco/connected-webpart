export interface ISourceWebPartProps {
  description: string;
  fieldComplete: boolean;
  items:Array<any>;
  selectedField: string;
  setSelectedItem: any;
  listTitle: string;
  selectedItem: any;
  htmlCode: string;
}
